import XCTest
@testable import SamplePackageMigrate

final class SamplePackageMigrateTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(SamplePackageMigrate().text, "Hello, World!")
    }
}
